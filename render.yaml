services:
  - type: web
    name: heat-mitigation-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
    healthCheckPath: /health
